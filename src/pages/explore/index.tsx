import React from 'react';
import CardGrid from '../../components/CardGrid';

const Home: React.FC = () => {
  return (
    <div className="w-screen min-h-screen h-full max-w-full bg-gray-900">
      <CardGrid />
    </div>
  );
};

export default Home;